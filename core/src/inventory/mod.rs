pub mod model;

pub use model::*;

// TODO: follow the `recipes` pattern once this resource is wired up:
// store.rs (list/get/create/update/delete against `inventory_items`) and
// seed.rs (port the sample INVENTORY from brew-data.jsx).
