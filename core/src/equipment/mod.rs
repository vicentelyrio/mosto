pub mod model;

pub use model::*;

// TODO: follow the `recipes` pattern once this resource is wired up:
// store.rs (list/get/create/update/delete against `equipment`) and seed.rs
// (port the sample EQUIPMENT from brew-data.jsx).
