/** Dark accent used for text/icons on top of the amber brand color — keeps
 *  contrast readable without falling back to plain black. Shared between the
 *  theme's button contrast override and the BrewLogo mark. */
export const ON_ACCENT = '#201504'
